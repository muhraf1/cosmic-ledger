version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd cosmic-ledgers
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - cd cosmic-ledgers
        - npm run build
  artifacts:
    baseDirectory: cosmic-ledgers/dist
    files:
      - '**/*'
  cache:
    paths:
      - 'cosmic-ledgers/node_modules/**/*'